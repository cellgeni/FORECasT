apiVersion: v1
kind: Service
metadata:
  name: mprofile-web
  labels:
    app: mprofile-web
spec:
  type: LoadBalancer
  ports:
  - port: 8005
    targetPort: mprofile-web
  selector:
    app: mprofile-web

